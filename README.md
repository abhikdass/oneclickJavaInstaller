# oneclickJavaInstaller
A one-click portable Java JDK 17 setup script for Windows that automatically downloads, extracts, configures, and verifies Java — no MSI installer, no Oracle login, no GitHub dependency issues.
